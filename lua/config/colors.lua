-- set comment color to be something else other than grey
-- vim.api.nvim_set_hl(0, "Comment", { fg = "#0c8599", bold = true, italic = true })

-- set transparency for different plugins
-- This is not needed, as catppuccin will do
-- this if it's set to be transparent
